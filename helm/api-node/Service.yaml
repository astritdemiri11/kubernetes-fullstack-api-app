apiVersion: v1
kind: Service
metadata:
  name: api-node
  namespace: kubernetes-fullstack-api-app
spec:
  selector:
    app: api-node
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
